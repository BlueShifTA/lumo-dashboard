# Backend
API_HOST=0.0.0.0
API_PORT=8002
DEBUG=true

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8002

# Optional database (only if you add persistence)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Optional auth (not required for Tailscale-only deployments)
# Generate a real secret before enabling auth, e.g.:
# JWT_SECRET=replace-with-32+-char-random-secret
# JWT_ALGORITHM=HS256
